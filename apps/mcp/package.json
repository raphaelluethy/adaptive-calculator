{
  "name": "shell-executor-mcp-server",
  "version": "1.0.0",
  "description": "MCP server that executes shell commands and returns output, execution time, and exit code",
  "type": "module",
  "main": "./build/index.js",
  "bin": {
    "shell-executor-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && chmod +x build/index.js",
    "start": "node build/index.js",
    "dev": "tsc && node build/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "files": [
    "build"
  ],
  "keywords": [
    "mcp",
    "shell",
    "executor",
    "model-context-protocol"
  ],
  "author": "Your Name",
  "license": "MIT"
}
